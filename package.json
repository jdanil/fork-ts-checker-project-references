{
  "name": "root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "compile": "lerna run compile"
  },
  "devDependencies": {
    "lerna": "3.22.0"
  }
}
